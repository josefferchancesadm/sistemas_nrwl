import { clientesInterfaces } from './clientes-interfaces';

describe('clientesInterfaces', () => {
  it('should work', () => {
    expect(clientesInterfaces()).toEqual('clientes-interfaces');
  });
});
